
# Bitnami configuration file for log rotation.
# To enable logrotate create a symlink to the system logrotate folder
#
# For example, in Ubuntu:
#
# sudo ln -s /opt/wordpress/config/logrotate/bitnami.conf /etc/logrotate.d/bitnami.conf
#

include /opt/wordpress/config/logrotate/logrotate.d/
